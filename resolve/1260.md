 # DFS와 BFS
Created at : 2026-01-17 21:47
[baekjoon_1260](https://www.acmicpc.net/problem/1260)
### 조건
- 시간 : 2
- 메모리 : 128
### 문제
그래프를 DFS로 탐색한 결과와 BFS로 탐색한 결과를 출력하는 프로그램을 작성하시오. 단, 방문할 수 있는 정점이 여러 개인 경우에는 정점 번호가 작은 것을 먼저 방문하고, 더 이상 방문할 수 있는 점이 없는 경우 종료한다. 정점 번호는 1번부터 N번까지이다.
### 입력
첫째 줄에 정점의 개수 N(1 ≤ N ≤ 1,000), 간선의 개수 M(1 ≤ M ≤ 10,000), 탐색을 시작할 정점의 번호 V가 주어진다. 다음 M개의 줄에는 간선이 연결하는 두 정점의 번호가 주어진다. 어떤 두 정점 사이에 여러 개의 간선이 있을 수 있다. 입력으로 주어지는 간선은 양방향이다.
### 출력
첫째 줄에 DFS를 수행한 결과를, 그 다음 줄에는 BFS를 수행한 결과를 출력한다. V부터 방문된 점을 순서대로 출력하면 된다.
### 제한
- 
### 예제
- 입력
```
4 5 1
1 2
1 3
1 4
2 4
3 4
```
- 출력
```
1 2 4 3
1 2 3 4
``` 
- 입력
```
5 5 3
5 4
5 2
1 2
3 4
3 1
```
- 출력
```
3 1 2 5 4
3 1 4 2 5
``` 
- 입력
```
1000 1 1000
999 1000
```
- 출력
```
1000 999
1000 999
``` 

### 풀이
- My solution
```python
from collections import deque
import sys
N,M,V = map(int,input().split())
graph = {i:[] for i in range(1,N+1)}
for _ in range(M):
    v,u = map(int,sys.stdin.readline().strip().split())
    graph[v].append(u)
    graph[u].append(v)

ret_dfs = []
queue = deque()
def dfs(R):
    queue.append(R)
    while queue:
        q = queue.popleft()
        if q not in ret_dfs:
            ret_dfs.append(q)
            queue.extendleft(sorted(graph[q], reverse=True))
dfs(V)
print(" ".join(str(ret) for ret in ret_dfs))

ret_bfs = []
def bfs(R):
    queue.append(R)
    while queue:
        q = queue.popleft()
        if q not in ret_bfs:
            ret_bfs.append(q)
            queue.extend(sorted(graph[q]))
bfs(V)
print(" ".join(str(ret) for ret in ret_bfs))
```

- Answer
```python

```

- Analyze
	- 단순한 dfs, bfs 문제이다. bfs, dfs의 차이는 queue에 넣는 차이란 것을 생각하자.
	- queueleft로 넣을 때, list의 역순으로 들어가기에 reverse를 해 주었다.
		- 내림차순으로 넣어야 queue에선 오름차순으로 들어간다.
### 알고리즘 분류
- 