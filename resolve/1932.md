 # 정수 삼각형
Created at : 2025-11-16 22:08
[baekjoon_1932](https://www.acmicpc.net/problem/1932)
### 조건
- 시간 : 2
- 메모리 : 128
### 문제
```
        7
      3   8
    8   1   0
  2   7   4   4
4   5   2   6   5
```
위 그림은 크기가 5인 정수 삼각형의 한 모습이다.

맨 위층 7부터 시작해서 아래에 있는 수 중 하나를 선택하여 아래층으로 내려올 때, 이제까지 선택된 수의 합이 최대가 되는 경로를 구하는 프로그램을 작성하라. 아래층에 있는 수는 현재 층에서 선택된 수의 대각선 왼쪽 또는 대각선 오른쪽에 있는 것 중에서만 선택할 수 있다.

삼각형의 크기는 1 이상 500 이하이다. 삼각형을 이루고 있는 각 수는 모두 정수이며, 범위는 0 이상 9999 이하이다.
### 입력
첫째 줄에 삼각형의 크기 n(1 ≤ n ≤ 500)이 주어지고, 둘째 줄부터 n+1번째 줄까지 정수 삼각형이 주어진다.
### 출력
첫째 줄에 합이 최대가 되는 경로에 있는 수의 합을 출력한다.
### 제한
- 
### 예제
- 입력
```
5
7
3 8
8 1 0
2 7 4 4
4 5 2 6 5
```
- 출력
```
30
``` 

### 풀이
- My solution
```python
n = int(input())
arr = []
for _ in range(n):
    tmp_arr = list(map(int,input().split()))
    arr.append(tmp_arr)

ret = arr[0]
for i in range(1, n):
    tmp_ret = []
    for j in range(len(arr[i])):
        sel = 0
        if j == 0:
            sel = ret[0]
        elif j == len(arr[i])-1:
            sel = ret[-1]
        else :
            sel = max(ret[j-1],ret[j])
        tmp_ret.append(arr[i][j] + sel)
    ret = tmp_ret

print(max(ret))
```

- Answer
```python

```

- Analyze
	- 이전 문제의 연장선 같은 문제였다.([[1149]])
	- 처음과 마지막만 선택권이 없고 (삼각형 특성상), 중간 부분에서는 이전 결과의 인덱스가 하나가 적기 때문에, j-1과 j 값 중, max를 ret에 저장해 주었다.
### 알고리즘 분류
- 다이나밍 프로그래밍