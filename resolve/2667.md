 # 단지번호붙이기
Created at : 2026-01-18 19:32
[baekjoon_2667](https://www.acmicpc.net/problem/2667)
### 조건
- 시간 : 1
- 메모리 : 128
### 문제
<그림 1>과 같이 정사각형 모양의 지도가 있다. 1은 집이 있는 곳을, 0은 집이 없는 곳을 나타낸다. 철수는 이 지도를 가지고 연결된 집의 모임인 단지를 정의하고, 단지에 번호를 붙이려 한다. 여기서 연결되었다는 것은 어떤 집이 좌우, 혹은 아래위로 다른 집이 있는 경우를 말한다. 대각선상에 집이 있는 경우는 연결된 것이 아니다. <그림 2>는 <그림 1>을 단지별로 번호를 붙인 것이다. 지도를 입력하여 단지수를 출력하고, 각 단지에 속하는 집의 수를 오름차순으로 정렬하여 출력하는 프로그램을 작성하시오.

![](https://www.acmicpc.net/upload/images/ITVH9w1Gf6eCRdThfkegBUSOKd.png)
### 입력
첫 번째 줄에는 지도의 크기 N(정사각형이므로 가로와 세로의 크기는 같으며 5≤N≤25)이 입력되고, 그 다음 N줄에는 각각 N개의 자료(0혹은 1)가 입력된다.
### 출력
첫 번째 줄에는 총 단지수를 출력하시오. 그리고 각 단지내 집의 수를 오름차순으로 정렬하여 한 줄에 하나씩 출력하시오.
### 제한
- 
### 예제
- 입력
```
7
0110100
0110101
1110101
0000111
0100000
0111110
0111000
```
- 출력
```
3
7
8
9
``` 

### 풀이
- My solution
```python
from collections import deque

N = int(input())

graph = []
for i in range(N):
    tmp_arr = list(map(int,input()))
    
    graph.append(tmp_arr)

check_graph = [[False]*N for _ in range(N)] 
queue = deque()

def searching_e(R):
    tmp_arr = []
    if R[0] != 0 and graph[R[0]-1][R[1]]:
        tmp_arr.append((R[0]-1, R[1]))
    if R[1] != 0 and graph[R[0]][R[1]-1]:
        tmp_arr.append((R[0], R[1]-1))
    if R[0] != N-1 and graph[R[0]+1][R[1]]:
        tmp_arr.append((R[0]+1, R[1]))
    if R[1] != N-1 and graph[R[0]][R[1]+1]:
        tmp_arr.append((R[0], R[1]+1))
    return tmp_arr

def bfs(cnt, R):
    queue.append(R)
    while queue:
        q = queue.popleft()
        if not check_graph[q[0]][q[1]]:
            cnt += 1
            check_graph[q[0]][q[1]] = True
            queue.extend(searching_e(q))
    return cnt
ret = []
for i in range(N):
    for j in range(N):
        if graph[i][j] != 0 and not check_graph[i][j]:
            ret.append(bfs(0,(i,j)))

print(len(ret))
print("\n".join(str(r) for r in sorted(ret)))
```

- Answer
```python

```

- Analyze
	- bfs든 dfs든 상관 없는 것으로 보여 속도가 좀 더 빠른 bfs로 구현
	- 기존과 다른 점은, 인접 graph가 1인 것을 확인하는 searching_e function을 추가.
### 알고리즘 분류
- 