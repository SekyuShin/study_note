 # LCS
Created at : 2025-11-25 22:54
[baekjoon_9251](https://www.acmicpc.net/problem/9251)
### 조건
- 시간 : 0.1
- 메모리 : 256
### 문제
LCS(Longest Common Subsequence, 최장 공통 부분 수열)문제는 두 수열이 주어졌을 때, 모두의 부분 수열이 되는 수열 중 가장 긴 것을 찾는 문제이다.

예를 들어, ACAYKP와 CAPCAK의 LCS는 ACAK가 된다.
### 입력
첫째 줄과 둘째 줄에 두 문자열이 주어진다. 문자열은 알파벳 대문자로만 이루어져 있으며, 최대 1000글자로 이루어져 있다.
### 출력
첫째 줄에 입력으로 주어진 두 문자열의 LCS의 길이를 출력한다.
### 제한
- 
### 예제
- 입력
```
ACAYKP
CAPCAK
```
- 출력
```
4
``` 

### 풀이
- My solution
```python
A = input()
B = input()

lcs = [[0]*(len(B)+1) for _ in range(len(A)+1)]
for i in range(1, len(A)+1):
    for j in range(1,len(B)+1):
        if A[i-1] == B[j-1]:
            lcs[i][j] = lcs[i-1][j-1] + 1
        else:
            lcs[i][j] = max(lcs[i-1][j], lcs[i][j-1])

# for i in range(1, len(A)+1):
#     for j in range(1,len(B)+1):
#         print(lcs[i][j],end="")
#     print()

print(lcs[len(A)][len(B)])
```

- Answer
```python

```

- Analyze
	- 이번 문제도 감을 영 못 잡아서 컨닝을 좀 했다.
	- 2차 배열로 만들어서 같은 값이 나오면 대각선 방향 (이전 기록) +1을 해주고 같은 값이 아니더라도 다음 계상을 위한 이전 기록을 유지하기 위해 왼쪽 및 위 방향의 기록을 유지해준다.
	- 이 부분은 다시 풀어보자.
### 알고리즘 분류
- 