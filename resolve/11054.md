 # 가장 긴 바이토닉 부분 수열
Created at : 2025-11-22 23:32
[baekjoon_11054](https://www.acmicpc.net/problem/11054)
### 조건
- 시간 : 1
- 메모리 : 256
### 문제
수열 S가 어떤 수 Sk를 기준으로 S1 < S2 < ... Sk-1 < Sk > Sk+1 > ... SN-1 > SN을 만족한다면, 그 수열을 바이토닉 수열이라고 한다.

예를 들어, {10, 20, **30**, 25, 20}과 {10, 20, 30, **40**}, {**50**, 40, 25, 10} 은 바이토닉 수열이지만, {1, 2, 3, 2, 1, 2, 3, 2, 1}과 {10, 20, 30, 40, 20, 30} 은 바이토닉 수열이 아니다.

수열 A가 주어졌을 때, 그 수열의 부분 수열 중 바이토닉 수열이면서 가장 긴 수열의 길이를 구하는 프로그램을 작성하시오.
### 입력
첫째 줄에 수열 A의 크기 N이 주어지고, 둘째 줄에는 수열 A를 이루고 있는 Ai가 주어진다. (1 ≤ N ≤ 1,000, 1 ≤ Ai ≤ 1,000)
### 출력
첫째 줄에 수열 A의 부분 수열 중에서 가장 긴 바이토닉 수열의 길이를 출력한다.
### 제한
- 
### 예제
- 입력
```
10
1 5 2 1 4 3 4 5 2 1
```
- 출력
```
7
``` 

### 풀이
- My solution
```python
n = int(input())
arr = list(map(int,input().split()))
ret_inc = [0]*n
ret_dec = [0]*n
for k in range(n):
    for i in range(k-1,-1,-1):
        if arr[k] > arr[i]:
            ret_inc[k] = max(ret_inc[k],ret_inc[i])
    ret_inc[k] += 1
for k in range(n-1,-1,-1):
    for j in range(k+1,n):
        if arr[k] > arr[j]:
            ret_dec[k] = max(ret_dec[k],ret_dec[j])
    ret_dec[k] += 1
ret_max = 0
for k in range(n):
    if ret_max < ret_inc[k]+ret_dec[k]:
        ret_max = ret_inc[k]+ret_dec[k]

print(ret_max-1)
```

- Answer
```python

```

- Analyze
	- 가장 긴 증가하는 부분 ([[11053]])과 매우 유사한 문제였다. 다만 다른 점은 바이토닉 수열이라는 부분인데 이걸 위해서 추가로 감소하는 수열을 구했다. 그리고 나서 결과값들을 더해주었는데, 이때 두 ret 모두 자기자신을 포함하는 숫자이기에 1을 빼 주었다.
### 알고리즘 분류
- 다이나믹 프로그래밍