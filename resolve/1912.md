 # 연속합
Created at : 2025-11-13 22:44
[baekjoon_1912](https://www.acmicpc.net/problem/1912)
### 조건
- 시간 : 1
- 메모리 : 128
### 문제
n개의 정수로 이루어진 임의의 수열이 주어진다. 우리는 이 중 연속된 몇 개의 수를 선택해서 구할 수 있는 합 중 가장 큰 합을 구하려고 한다. 단, 수는 한 개 이상 선택해야 한다.

예를 들어서 10, -4, 3, 1, 5, 6, -35, 12, 21, -1 이라는 수열이 주어졌다고 하자. 여기서 정답은 12+21인 33이 정답이 된다.
### 입력
첫째 줄에 정수 n(1 ≤ n ≤ 100,000)이 주어지고 둘째 줄에는 n개의 정수로 이루어진 수열이 주어진다. 수는 -1,000보다 크거나 같고, 1,000보다 작거나 같은 정수이다.
### 출력
첫째 줄에 답을 출력한다.
### 제한
- 
### 예제
- 입력
```
10
10 -4 3 1 5 6 -35 12 21 -1
```
- 출력
```
33
``` 
- 입력
```
10
2 1 -4 3 4 -4 6 5 -5 1
```
- 출력
```
14
``` 
- 입력
```
5
-1 -2 -3 -4 -5
```
- 출력
```
-1
``` 
### 풀이
- My solution (Pypy3 - 156ms)
```python
n = int(input())
arr = list(map(int,input().split()))

ret = [0]*n
for i in range(n):
    if i == n-1:
        ret[n-1] = arr[n-1]
    sum = arr[i]
    ret[i] = arr[i]
    for j in range(i+1, n):
        sum += arr[j]
        if sum <= 0:
            break
        elif sum > ret[i]:
            ret[i] = sum
print(max(ret))
```
- My solution2 (C++ - 8ms)
```cpp
#include<cstdio>
#include<algorithm>

using namespace std;
int main(){
	int input;
	int result = -1000;
	int tmp_result = 0;
	int n;

	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf("%d", &input);
		if (tmp_result + input < 0) {
			if (tmp_result == 0) result = max(result, input);
			tmp_result = 0;
		} else {
			tmp_result += input;
			result = max(result, tmp_result);
		}
		
	}
	
	printf("%d\n", result);

	return 0;
}
```

- My solution3(python3 - 104ms)
```python
n = int(input())
arr = list(map(int,input().split()))

ret = -1000
tmp_ret = 0

for i in range(n):
    if tmp_ret + arr[i] < 0:
        if tmp_ret == 0 :
            ret = max(ret, arr[i])
        tmp_ret = 0
    else :
        tmp_ret += arr[i]
        ret = max(ret,tmp_ret)
print(ret)
```
- Answer
```python

```

- Analyze
	- pypy3로 하닌깐 통과되었다.
	- sum이 -가 되는 순간엔 더해도 그 뒤를 더해도 의미가 없다.
		- ex) 1 -4 3 4 => 1-4+3보다 무조건 3부터 시작하는게 더 크다.
	- My solution2 (C++) : 이전에 푼 풀이
		- 이전에 푼 방법이 좀 더 심플해 보인다. tmp_result를 중간에 두고, input값을 더하다가 -가 되는 시점에서 max값을 비교해서 저장하고 tmp_result를 초기화하는 방법이다.
		- for 문이 한번만 쓰이기에 빠르다.
	- My solution3 (python) : solution2 참고
		- 역시 훨씬 빠르다.
### 알고리즘 분류
- 