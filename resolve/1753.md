 # 최단경로
Created at : 2026-02-18 21:30
[baekjoon_1753](https://www.acmicpc.net/problem/1753)
### 조건
- 시간 : 1
- 메모리 : 256
### 문제
방향그래프가 주어지면 주어진 시작점에서 다른 모든 정점으로의 최단 경로를 구하는 프로그램을 작성하시오. 단, 모든 간선의 가중치는 10 이하의 자연수이다.
### 입력
첫째 줄에 정점의 개수 V와 간선의 개수 E가 주어진다. (1 ≤ V ≤ 20,000, 1 ≤ E ≤ 300,000) 모든 정점에는 1부터 V까지 번호가 매겨져 있다고 가정한다. 둘째 줄에는 시작 정점의 번호 K(1 ≤ K ≤ V)가 주어진다. 셋째 줄부터 E개의 줄에 걸쳐 각 간선을 나타내는 세 개의 정수 (u, v, w)가 순서대로 주어진다. 이는 u에서 v로 가는 가중치 w인 간선이 존재한다는 뜻이다. u와 v는 서로 다르며 w는 10 이하의 자연수이다. 서로 다른 두 정점 사이에 여러 개의 간선이 존재할 수도 있음에 유의한다.
### 출력
첫째 줄부터 V개의 줄에 걸쳐, i번째 줄에 i번 정점으로의 최단 경로의 경로값을 출력한다. 시작점 자신은 0으로 출력하고, 경로가 존재하지 않는 경우에는 INF를 출력하면 된다.
### 제한
- 
### 예제
- 입력
```
5 6
1
5 1 1
1 2 2
1 3 3
2 3 4
2 4 5
3 4 6
```
- 출력
```
0
2
3
7
INF
``` 

### 풀이
- My solution
```python
import heapq

V,E = map(int, input().split())
start = int(input())
graph = {v:{} for v in range(V+1)}
for _ in range(E):
    u,v,w = map(int,input().split())
    graph[u][v] = w
    
def dijkstra(graph,start):
    distances = {node:float('inf') for node in graph}
    distances[start] = 0
    priority_queue = [(0,start)]
    
    while priority_queue:
        cur_distance, cur_node = heapq.heappop(priority_queue)
        if cur_distance > distances[cur_node]:
            continue
        for near_node, weight in graph[cur_node].items():
            dist = cur_distance+weight
            if distances[near_node] > dist:
                distances[near_node] = dist
                heapq.heappush(priority_queue, (dist, near_node))
    return distances

distances = dijkstra(graph,start)
for d in range(1,V+1):
    print(distances[d])
```

- Answer
```python

```

- Analyze
	- [[다익스트라 알고리즘]]을 사용하여 풀어보았다.
	- priority queue를 사용하여 가중치에 대한 start 지점을 입력해준 다음 연관된 다른 노드들에 가중치를 더해가며 푸는 문제이다. (DP와 유사해 보인다.)
### 알고리즘 분류
- 