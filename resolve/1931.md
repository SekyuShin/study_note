 # 회의실 배정
Created at : 2025-12-08 23:26
[baekjoon_1931](https://www.acmicpc.net/problem/1931)
### 조건
- 시간 : 2
- 메모리 : 128
### 문제
한 개의 회의실이 있는데 이를 사용하고자 하는 N개의 회의에 대하여 회의실 사용표를 만들려고 한다. 각 회의 I에 대해 시작시간과 끝나는 시간이 주어져 있고, 각 회의가 겹치지 않게 하면서 회의실을 사용할 수 있는 회의의 최대 개수를 찾아보자. 단, 회의는 한번 시작하면 중간에 중단될 수 없으며 한 회의가 끝나는 것과 동시에 다음 회의가 시작될 수 있다. 회의의 시작시간과 끝나는 시간이 같을 수도 있다. 이 경우에는 시작하자마자 끝나는 것으로 생각하면 된다.
### 입력
첫째 줄에 회의의 수 N(1 ≤ N ≤ 100,000)이 주어진다. 둘째 줄부터 N+1 줄까지 각 회의의 정보가 주어지는데 이것은 공백을 사이에 두고 회의의 시작시간과 끝나는 시간이 주어진다. 시작 시간과 끝나는 시간은 2^31-1보다 작거나 같은 자연수 또는 0이다.
### 출력
첫째 줄에 최대 사용할 수 있는 회의의 최대 개수를 출력한다.
### 제한

### 예제
- 입력
```
11
1 4
3 5
0 6
5 7
3 8
5 9
6 10
8 11
8 12
2 13
12 14
```
- 출력
```
4
``` 
-  힌트
	- (1,4), (5,7), (8,11), (12,14) 를 이용할 수 있다.
### 풀이
- My solution
```python
N = int(input())
arr = []
for _ in range(N):
    arr.append(tuple(map(int,input().split())))

arr.sort(key=lambda x:x[0])

ret_max = -1
ret = [0]*N
for i in range(N):
    ret[i] = 1
    for j in range(i-1,-1,-1):
        if arr[j][1] < arr[i][0]:
            ret[i] = max(ret[j]+1,ret[i])
            if ret_max < ret[i]:
                ret_max = ret[i]

print(ret_max)
```

- My solution2
```python
N = int(input())
arr = []
for _ in range(N):
    arr.append(tuple(map(int,input().split())))

arr.sort(key=lambda x:(x[1],x[0]))

# print(arr)
cnt = 1
point = arr[0][1]

for i in range(1,len(arr)):
    if point <= arr[i][0]:
        cnt+=1
        point = arr[i][1]

print(cnt)
```
- Answer
```python

```

- Analyze
	- My solution1 : 누적합으로 구해 보았다 다만, 시간초과.. 좀 더 생각해보자.
	- My solution2 
		- sort를 끝나는 시간으로 하고 시작시간과 비교하여 카운트를 해주었다.
		- 끝나는 시간이 중복될 땐, 시작시간 순으로 정렬 (크게 의미는 없는거 같다.)
		- 반복문이 1회만 돌기에 훨씬 빠르다.
		- 일단 풀이를 보면, 끝나는 시간 순으로 정렬했기에 가장 빠르게 끝나는 회의가 선택된다. 이후, 시작시간이 현재 끝난 시간보다 이후면 count해 준다. 이때 역시 끝나는 시간 순이기에 가장 빠르게 끝난 회의가 count가 된다.
### 알고리즘 분류
- 