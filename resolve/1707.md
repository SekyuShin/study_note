 # 이분 그래프
Created at : 2026-02-05 22:23
[baekjoon_1707](https://www.acmicpc.net/problem/1707)
### 조건
- 시간 : 2
- 메모리 : 256
### 문제
그래프의 정점의 집합을 둘로 분할하여, 각 집합에 속한 정점끼리는 서로 인접하지 않도록 분할할 수 있을 때, 그러한 그래프를 특별히 이분 그래프 (Bipartite Graph) 라 부른다.

그래프가 입력으로 주어졌을 때, 이 그래프가 이분 그래프인지 아닌지 판별하는 프로그램을 작성하시오.
### 입력
입력은 여러 개의 테스트 케이스로 구성되어 있는데, 첫째 줄에 테스트 케이스의 개수 K가 주어진다. 각 테스트 케이스의 첫째 줄에는 그래프의 정점의 개수 V와 간선의 개수 E가 빈 칸을 사이에 두고 순서대로 주어진다. 각 정점에는 1부터 V까지 차례로 번호가 붙어 있다. 이어서 둘째 줄부터 E개의 줄에 걸쳐 간선에 대한 정보가 주어지는데, 각 줄에 인접한 두 정점의 번호 u, v (u ≠ v)가 빈 칸을 사이에 두고 주어진다.
### 출력
K개의 줄에 걸쳐 입력으로 주어진 그래프가 이분 그래프이면 YES, 아니면 NO를 순서대로 출력한다.
### 제한
- 2 ≤ K ≤ 5
- 1 ≤ V ≤ 20,000
- 1 ≤ E ≤ 200,000
### 예제
- 입력
```
2
3 2
1 3
2 3
4 4
1 2
2 3
3 4
4 2
```
- 출력
```
YES
NO
``` 

### 풀이
- My solution (틀림)
```python
from collections import deque
import sys

T = int(sys.stdin.readline().strip())

def dfs(graph):
    queue = deque()
    queue.append(1)
    ret_list = [0] * (len(graph)+1)
    ret_list[1] = 1
    while queue:
        q = queue.popleft()
        for v in reversed(graph[q]):
            if ret_list[v] != 0 :
                if ret_list[v] == 2:
                    return "NO"
                else:
                    ret_list[v] +=1
            else:
                ret_list[v] +=1
                queue.appendleft(v)
    return "YES"

for _ in range(T):
    V,E = map(int,sys.stdin.readline().strip().split())
    graph = {i:[] for i in range(1,V+1)}
    for e in range(E):
        u, v = map(int,sys.stdin.readline().strip().split())
        graph[u].append(v)
        graph[v].append(u)

    print(dfs(graph))
```
- My solution 2 (틀림림)
```python
from collections import deque
import sys

T = int(sys.stdin.readline().strip())

def dfs(graph, V):
    queue = deque()
    queue.append(1)
    ret_list = [-1] * (len(graph)+1)
    ret_list[1] = 0
    while queue:
        q = queue.popleft()
        for v in reversed(graph[q]):
            if ret_list[v] == -1 :
                ret_list[v] = (ret_list[q]+1)%2
                queue.appendleft(v)
            elif ret_list[v] == ret_list[q]:
                return "NO"
    return "YES"

for _ in range(T):
    V,E = map(int,sys.stdin.readline().strip().split())
    graph = {i:[] for i in range(1,V+1)}
    for e in range(E):
        u, v = map(int,sys.stdin.readline().strip().split())
        graph[u].append(v)
        graph[v].append(u)

    print(dfs(graph, V))
```

- My solution3 
```python

from collections import deque
import sys

T = int(sys.stdin.readline().strip())

def dfs(graph, V):
    ret_list = [-1] * (len(graph)+1)
    

    for start in range(1,V+1):
        if ret_list[start] == -1:
            queue = deque([start])
            ret_list[start] = 0
            while queue:
                q = queue.popleft()
                for v in reversed(graph[q]):
                    if ret_list[v] == -1 :
                        ret_list[v] = (ret_list[q]+1)%2
                        queue.appendleft(v)
                    elif ret_list[v] == ret_list[q]:
                        return "NO"
    return "YES"

for _ in range(T):
    V,E = map(int,sys.stdin.readline().strip().split())
    graph = {i:[] for i in range(1,V+1)}
    for e in range(E):
        u, v = map(int,sys.stdin.readline().strip().split())
        graph[u].append(v)
        graph[v].append(u)

    print(dfs(graph, V))
        
```
- Answer
```python

```

- Analyze
	- My Solution1
		- 이분 그래프는 그냥 동그랗게 사이클이 돌지 않으면 되는 것 같다.
		- 해당 부분을 확인하기 위해 ret_list에서 True / False가 아닌 count로 해보았다.
		- 결과는 실패.. 좀 더 생각해보자
	- My Solution2
		- 이분 그래프는 인접한 정점이 서로 다른 부분에 있으면 되는 것 같다.
		- 아래를 토대로 ret_list에 0,1로 구분해서 결과를 입력해 주었다.
		- 이분 그래프 성립
			- ![[Pasted image 20260208020500.png]]
			- ![[Pasted image 20260208020513.png]]
		- 이분 그래프 성립 x
			- ![[Pasted image 20260208020535.png]]
			- ![[Pasted image 20260208020545.png]]
		- 틀렸다. 아마 그래프가 떨어져 있는 경우도 있는 거 같다.
	- My solution3
		- 결국 성공 모든 정점에 대해 확인하는 알고리즘을 추가해 주었다.
### 알고리즘 분류
- 