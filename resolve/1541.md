 # 잃어버린 괄호
Created at : 2025-12-11 21:51
[baekjoon_1541](https://www.acmicpc.net/problem/1541)
### 조건
- 시간 : 2
- 메모리 : 128
### 문제
세준이는 양수와 +, -, 그리고 괄호를 가지고 식을 만들었다. 그리고 나서 세준이는 괄호를 모두 지웠다.

그리고 나서 세준이는 괄호를 적절히 쳐서 이 식의 값을 최소로 만들려고 한다.

괄호를 적절히 쳐서 이 식의 값을 최소로 만드는 프로그램을 작성하시오.
### 입력
첫째 줄에 식이 주어진다. 식은 ‘0’~‘9’, ‘+’, 그리고 ‘-’만으로 이루어져 있고, 가장 처음과 마지막 문자는 숫자이다. 그리고 연속해서 두 개 이상의 연산자가 나타나지 않고, 5자리보다 많이 연속되는 숫자는 없다. 수는 0으로 시작할 수 있다. 입력으로 주어지는 식의 길이는 50보다 작거나 같다.
### 출력
첫째 줄에 정답을 출력한다.
### 제한
- 
### 예제
- 입력
```
55-50+40
```
- 출력
```
-35
``` 
- 입력
```
10+20+30+40
```
- 출력
```
100
``` 

- 입력
```
00009-00009
```
- 출력
```
0
``` 

### 풀이
- My solution
```python
arr = input()

ret = 0
value = ""
check_minus = False
for i in range(len(arr)):
    if arr[i] in ['+','-']:
        num = int(value)
        value = ""
        if check_minus:
            ret -= num
        else:
            ret += num
        if arr[i] == '-':
            check_minus = True
    else:
        value += arr[i]
        if i == len(arr)-1 :
            if check_minus:
                ret -= int(value)
            else:
                ret += int(value)

print(ret)
```

- Answer
```python

```

- Analyze
	- 생각보다 쉽게 풀었다. 일단 괄호를 적절히 쳐서 최소로 만들어야 하는데, 조금만 생각해보면, -가 나온 시점 이후로 괄호를 쳐버리면 전부 -로 계산할 수 있다.
	- 예를 들면 3+15-1+2+6-10+1+2+3 이라면, 3+15-(1+2+6)-(10+1+2)이런 식으로 무조건적으로 음수로 만들 수 있다. 
	- 요건은 -가 되는 시점까지 더하다 -가 나온 시점부터 전부 빼면 된다.
### 알고리즘 분류
- 