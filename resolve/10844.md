 # 쉬운 계단 수
Created at : 2025-11-19 22:33
[baekjoon_10844](https://www.acmicpc.net/problem/10844)
### 조건
- 시간 : 1
- 메모리 : 256
### 문제
45656이란 수를 보자.

이 수는 인접한 모든 자리의 차이가 1이다. 이런 수를 계단 수라고 한다.

N이 주어질 때, 길이가 N인 계단 수가 총 몇 개 있는지 구해보자. 0으로 시작하는 수는 계단수가 아니다.
### 입력
첫째 줄에 N이 주어진다. N은 1보다 크거나 같고, 100보다 작거나 같은 자연수이다.
### 출력
첫째 줄에 정답을 1,000,000,000으로 나눈 나머지를 출력한다.
### 제한
- 
### 예제
- 입력
```
1
```
- 출력
```
9
``` 
- 입력
```
2
```
- 출력
```
17
``` 

### 풀이
- My solution
```python
n = int(input())

arr = [1]*10
arr[0] = 0
for _ in range(1, n):
    tmp_arr = [0]*10
    for i in range(0,10):
        if i == 0:
            tmp_arr[1] += arr[0]
        elif i == 9:
            tmp_arr[8] += arr[9]        
        else:
            tmp_arr[i-1] += arr[i]
            tmp_arr[i+1] += arr[i]
    arr = tmp_arr
```

- Answer
```python

```

- Analyze
	- 곰곰이 생각해 보면, 이전 결과에 대해 추가해 주는 방식이다.
	![[Pasted image 20251119230554.png]]
	- 위의 그림과 같이 뒤가 1로 끝나면 가능한 수는 0,1이다.
	- 이걸 토대로 0-9까지 마지막 숫자들의 개수를 저장하고 다음 계산에서 해당 숫자만큼 인접 숫자에 더해주었다. (0과 9는 인접숫자는 1개만 있으므로 예외처리를 해주었다.)
### 알고리즘 분류
- 