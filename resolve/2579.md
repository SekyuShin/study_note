 # 계단 오르기
Created at : 2025-11-17 23:09
[baekjoon_2579](https://www.acmicpc.net/problem/2579)
### 조건
- 시간 : 1
- 메모리 : 128
### 문제
계단 오르기 게임은 계단 아래 시작점부터 계단 꼭대기에 위치한 도착점까지 가는 게임이다. <그림 1>과 같이 각각의 계단에는 일정한 점수가 쓰여 있는데 계단을 밟으면 그 계단에 쓰여 있는 점수를 얻게 된다.

![](https://u.acmicpc.net/7177ea45-aa8d-4724-b256-7b84832c9b97/Screen%20Shot%202021-06-23%20at%203.00.46%20PM.png)

<그림 1>

예를 들어 <그림 2>와 같이 시작점에서부터 첫 번째, 두 번째, 네 번째, 여섯 번째 계단을 밟아 도착점에 도달하면 총 점수는 10 + 20 + 25 + 20 = 75점이 된다.

![](https://u.acmicpc.net/f00b6121-1c25-492e-9bc0-d96377c586b0/Screen%20Shot%202021-06-23%20at%203.01.39%20PM.png)

<그림 2>

계단 오르는 데는 다음과 같은 규칙이 있다.

1. 계단은 한 번에 한 계단씩 또는 두 계단씩 오를 수 있다. 즉, 한 계단을 밟으면서 이어서 다음 계단이나, 다음 다음 계단으로 오를 수 있다.
2. 연속된 세 개의 계단을 모두 밟아서는 안 된다. 단, 시작점은 계단에 포함되지 않는다.
3. 마지막 도착 계단은 반드시 밟아야 한다.

따라서 첫 번째 계단을 밟고 이어 두 번째 계단이나, 세 번째 계단으로 오를 수 있다. 하지만, 첫 번째 계단을 밟고 이어 네 번째 계단으로 올라가거나, 첫 번째, 두 번째, 세 번째 계단을 연속해서 모두 밟을 수는 없다.

각 계단에 쓰여 있는 점수가 주어질 때 이 게임에서 얻을 수 있는 총 점수의 최댓값을 구하는 프로그램을 작성하시오.
### 입력
입력의 첫째 줄에 계단의 개수가 주어진다.

둘째 줄부터 한 줄에 하나씩 제일 아래에 놓인 계단부터 순서대로 각 계단에 쓰여 있는 점수가 주어진다. 계단의 개수는 300이하의 자연수이고, 계단에 쓰여 있는 점수는 10,000이하의 자연수이다.
### 출력
첫째 줄에 계단 오르기 게임에서 얻을 수 있는 총 점수의 최댓값을 출력한다.
### 제한
- 
### 예제
- 입력
```
6
10
20
15
25
10
20
```
- 출력
```
75
``` 

### 풀이
- My solution 1
```python
n = int(input())
arr = []
ret = [0]*n
for _ in range(n):
    arr.append(int(input()))
ret[0] = arr[0]
ret[1] = arr[1]

for i in range(n-1):
    if i == n-3:
        ret[n-1] = max(ret[n-1],ret[n-3]+arr[n-1])
    elif i == n-2:
        ret[n-1] = max(ret[n-1],ret[n-2]+arr[n-1])
    else:
        ret[i+2] = max(ret[i+2],ret[i]+arr[i+2])
        ret[i+3] = max(ret[i+3],ret[i]+arr[i+1]+arr[i+3])

print(ret[-1])
```

- My solution 2
```python

n = int(input())
arr = []
ret = [0]*n
for _ in range(n):
    arr.append(int(input()))

if n == 1:
    ret[0] = arr[0]
elif n == 2:
    ret[0] = arr[0]
    ret[1] = arr[1] + arr[0]
elif n == 3:
    ret[2] = max(arr[0]+arr[2],arr[1]+arr[2])
elif n == 4:
    ret[3] = max(arr[0]+arr[2]+arr[3],arr[1]+arr[3])
else:
    ret[0] = arr[0]
    ret[1] = arr[1]

    for i in range(n-3):
        if i == n-3:
            ret[n-1] = max(ret[n-1],ret[n-3]+arr[n-1])
        elif i == n-2:
            ret[n-1] = max(ret[n-1],ret[n-2]+arr[n-1])
        else:
            ret[i+2] = max(ret[i+2],ret[i]+arr[i+2])
            ret[i+3] = max(ret[i+3],ret[i]+arr[i+1]+arr[i+3])

print(ret[-1])
        
```
- My solution3
```python
n = int(input())
arr = []
ret = [0]*n
for _ in range(n):
    arr.append(int(input())) 

for i in range(n):
    if i == 0:
        ret[0] = arr[0]
    elif i == 1:
        ret[1] = arr[1] + arr[0]
    elif i == 2:
        ret[2] = max(arr[0],arr[1])+arr[2]
    else :
        ret[i] = max(ret[i-3] + arr[i-1], ret[i-2]) + arr[i]


print(ret[-1])
```
- Answer
```python

```

- Analyze
	- 신박하다. My solution1에서 97퍼까지 가고 index error가 나와서 생각을 해보니 예외처리가 부족한 거 같아서 추가해주었다. 근데 예외처리한 My solution2에서 3퍼 틀림..
	  뭘까
	- 아직도 My Solution1에서는 97% 까지 정답에 근접했는지는 모르겠다.
	- My Solution3
		- 관점을 바꾸어서 i가 무조건 들어갈 수 있는 경우 중, 최댓값을 구해 주었다.
			- i번째 계단을 밟을 수 있는 경우는 두 가지로
				- i-1번 계단을 밟고 이어서 i번째 계단을 밟을 수 있다.
					- 필연적으로 i-3번 계단을 밟고 i-1번 계단을 밟게 된다.
					- 이때, i-3번 이전에 어떤 계단을 밟았는지는 상관없다.
				- i-2번 계단을 밝고 이어서 i번째 계단을 밟을 수 있다.
					- 이때, i-2번 계단 이전에 어떤 계단을 밟았는지는 상관없다.
		- 이러한 계산식으로 코딩을 진행했다. (i-3번 이전엔 별도의 계산이 필요해서 예외처리)
	
### 알고리즘 분류
- 