 # 알고리즘 수업 - 피보나치 수 1
Created at : 2025-11-09 17:32
[baekjoon_24416](https://www.acmicpc.net/problem/24416)
### 조건
- 시간 : 1
- 메모리 : 512
### 문제
오늘도 서준이는 동적 프로그래밍 수업 조교를 하고 있다. 아빠가 수업한 내용을 학생들이 잘 이해했는지 문제를 통해서 확인해보자.

오늘은 n의 피보나치 수를 재귀호출과 동적 프로그래밍으로 구하는 알고리즘을 배웠다. 재귀호출에 비해 동적 프로그래밍이 얼마나 빠른지 확인해 보자. 아래 의사 코드를 이용하여 n의 피보나치 수를 구할 경우 코드1 코드2 실행 횟수를 출력하자.

피보나치 수 재귀호출 의사 코드는 다음과 같다.
```
fib(n) {
    if (n = 1 or n = 2)
    then return 1;  # 코드1
    else return (fib(n - 1) + fib(n - 2));
}
```
```
fibonacci(n) {
    f[1] <- f[2] <- 1;
    for i <- 3 to n
        f[i] <- f[i - 1] + f[i - 2];  # 코드2
    return f[n];
}
```

피보나치 수 동적 프로그래밍 의사 코드는 다음과 같다.
### 입력
첫째 줄에 _n_(5 ≤ _n_ ≤ 40)이 주어진다.
### 출력
코드1 코드2 실행 횟수를 한 줄에 출력한다.
### 제한
- 
### 예제
- 입력
```
5
```
- 출력
```
5 3
``` 
- 입력
```
30
```
- 출력
```
832040 28
``` 

### 풀이
- My solution
```python
# def fib(n):
#     if n in [1,2]:
#         return 1
#     return fib(n-1) + fib(n-2)

def fibonocci(n):
    f = [1,1]
    cnt = 0
    for i in range(2,n):
        cnt+=1
        f.append(f[i-1]+f[-2])
    return f[n-1], cnt

n = int(input())
ret = fibonocci(n)
print(f"{ret[0]} {ret[1]}")
```

- Answer
```python

```

- Analyze
	- 이건 뭐.. 재귀함수의 실행 횟수를 구하라길래 그대로 했더니 시간초과가 나왔다.
	- 어짜피 결과 = 재귀함수 실행 횟수이기에 DP로 구한 값과 cnt를 출력해서 풀었다.
### 알고리즘 분류
- 수학
- 다이나믹 프로그래밍